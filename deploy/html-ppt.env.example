# 复制为 /etc/html-ppt.env（或项目目录下的 .env），并把权限设为 600
NVIDIA_API_KEY=在这里填写密钥
# 可选：到 build.nvidia.com 挑选中文表现更好的模型后替换
NVIDIA_MODEL=meta/llama-3.3-70b-instruct
PORT=8080
HOST=127.0.0.1
TRUST_PROXY=1
RATE_LIMIT=6
